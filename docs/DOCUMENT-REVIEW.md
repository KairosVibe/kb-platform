# R3 文档审查与交付记录

日期：2026-09-16。审查对象为文档和图表，不是业务代码。

## 1. 已完成

- 原四张架构/流程图重新生成，并增加流式恢复、文档交付、问答结果分流三张，共七张；每张同时提供JSON与HTML。
- 新增API协议及48项F函数HTTP登记、7项页面补充入口，另明确H函数和异步适配入口；补齐用户角色列表、ACL回填、补档来源绑定及挖掘进度。
- 新增逻辑数据字典、唯一键/关联约束、事务与迁移边界；补充文件格式矩阵、建议评测门槛、ADR和图册导航。
- 修正登录前误用功能鉴权、SSE先存游标、终态与事件之间崩溃窗口、批量结果无法定位文件、手动重试缺revision、FAQ无法显式重审等文档问题。
- 保留原60用户故事、180验收项、60功能函数的编号；新增协议属于原模块配套设计，无电商业务扩展。

## 2. 用户已确认

通用知识管理与问答平台，用于工程落地和简历展示；首轮DOCX、文本PDF、Markdown、TXT，DOC/OCR后续扩展；演示采用云端模型API与模拟资料。当前继续停留文档阶段，未批准业务开发。

## 3. 实际验证

- 七份图源通过showcase交付检查，每份9项artifact检查，composition错误/警告均为0。
- 七份HTML通过1440×900、1600×1000、1920×1080、2048×1320显示检查，无水平/垂直溢出。
- 已查看浅色总览的最小/最大尺寸，并抽查系统架构及问答流程深色图。原始工具收据visualReview=pending保持原样，不伪造为自动人工通过。
- 检查文档相对链接、功能编号保留、HTTP登记唯一性、图节点/边引用及源文件/HTML交付哈希；验证摘要见图册证据目录的document-validation.json。

## 4. 仍待确认与未验证

- 参数确认（2026-09-16 更新）：部署机器与服务版本、保留期限、质量门槛、性能与恢复目标**已确认并冻结**（见 [格式与验收](FORMAT-ACCEPTANCE.md) 第4节 D-04—D-07）；embedding 维度冻结为 1024。仍待确认：**D-08 中间件承载方式**（涉及新增安装）、云模型供应商与具体模型ID及额度。
- API、数据字段和状态机为设计稿；OpenAPI与数据库迁移的生成/执行、编译、业务测试、模型质量评测、压测和恢复演练未执行。
- 现有权限代码的旁路与冲突已逐项登记并按审计步骤 1—6 修正：三条读权旁路、受限输出收敛、判定类型字段补齐（"数据读权"与"索引可用性"正交）、密码长度契约与 embedding 向量空间标识、HTTP 状态码与错误码两层模型（含响应 `request_id`）、旧文档编号引用改引 R3、四维 16 组合矩阵补全均完成（**PA-01—PA-05、PA-08—PA-15 关闭**；PA-06 判定侧关闭；PA-07 部分关闭，`pytest` 124 passed，门禁已知差异清零）。PA-06 与 PA-07 **已关闭（2026-09-16）**：`app/services/authz_svc.py`（H04 读库与分桶）与 `auth_svc.py`（`PersistentRefreshStore`）落地，36 表 ORM + Alembic 迁移已在真实 MySQL 8.0.26 执行，`pytest` **245 passed**（含 17 项集成用例：CHECK 真实拦截、FK RESTRICT/CASCADE、二进制排序规则、`DATETIME(6)` 微秒保真、**跨进程重启后撤销仍生效**、并发刷新仅一个成功、`authorize_units` 端到端分桶）。至此 **PA-01—PA-15 全部关闭**，D1 权限前置门禁已满足；但 API 层与前端尚未落地，仍不构成"平台可运行"结论。详见 [权限契约审计](PERMISSION-CONTRACT-AUDIT.md)。

结论：图册与配套契约已补齐本轮发现的文档缺口，可供下一轮用户审阅；不是“生产就绪”或“全部实现已验收”的结论。参数除 **D-08 中间件承载方式**外已冻结；D1 的启动仍取决于权限修正门禁（PA-01—PA-08）与 D-08。

## 5. 兼容与风险

原图和被修改文档在写回前备份。业务代码、运行依赖和数据库均未修改。后续实现需同步DTO消费者、任务表和事务边界，不能只改前端字段。现有7张图为概览与细节的组合，必须结合图册说明阅读被省略的依赖和回边。

## 6. 复用评估补充

新增REUSE-MATRIX与ADR-0004，重写过时REUSE_NOTES。已核对客户端自报角色、Mock重排降级、检索异常跳过、SSE无seq及文本解析忽略错误等源码行为。新建议为先验证原工程基线，再比较渐进改造与组件迁入；未运行基线、未修改任何业务代码，不给工期或复用比例承诺。

**B1 已于 2026-09-16 复验通过**（五项验收全部通过，在隔离副本 `baseline-b1/` 中进行，来源快照零修改）。根因由原来的"依赖镜像传输链路阻塞"更正为"**快照缺失 `README.md` 导致构建元数据阶段中止**"；此前的 TLS handshake EOF 属瞬时故障。另发现快照含真实凭据、原项目日志实现有缺陷（时间戳异常与 `%s` 未插值）。B2/B3 仍未运行，复用路线选择保持开放。详见 [基线验证](BASELINE-VALIDATION.md)。

## 7. 代码契约审计补充

第 4 节"现有权限代码仍有与新契约冲突的旁路"已展开为逐项审计：对 `backend/` 现有权限基座与 R3 安全契约做只读比对，记录 15 项差异（PA-01—PA-15），其中 8 项为安全红线（超管与创建者读权旁路、部门祖先链匹配、受限提示泄漏标题与 ACL 名单、缺失删除与版本状态判定、refresh 撤销未持久化、旧契约测试固化）；`pytest` 31 passed 属旧契约期望，不作为新契约证据。原 `scripts/check_doc_code_sync.py` 因锚点为旧文档格式而 exit 1（PA-13），已重写为"锚点存活自检 + 已知差异清单"并复跑 exit 0，可作为后续回归门禁。审计给出 D1 前置修正顺序与门禁，未修改任何业务代码。详见 [权限契约审计](PERMISSION-CONTRACT-AUDIT.md)。
