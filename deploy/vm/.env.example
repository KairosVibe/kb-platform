# 复制为 .env 后填入真实值；**.env 不进版本库**（DEPLOYMENT §2 第 4 条）
#
#   cp .env.example .env && vi .env
#
# 说明（2026-09-16 对齐 VM 实况）：
#   镜像版本在 compose 中**字面写死**（Milvus v2.5.5 / Neo4j 5.21.0 等），不使用变量——
#   因为这些是本机离线镜像包里实际存在的坐标，"用了哪个版本"应当可直接从文件读到，
#   而不是依赖一个可能为空的变量。故本文件只保留口令一项。

# Neo4j 口令：Neo4j 5 要求 ≥8 字符。示例值仅用于说明格式，**必须更换**。
# VM 上 `/root/rag/.env` 的该值长度实测为 13 字符。
NEO4J_PASSWORD=change-me-to-a-real-password
